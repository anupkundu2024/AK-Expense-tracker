// This file is no longer used - demo users have been removed for production
// Users must now sign up manually through the application

const seedUsers = async () => {
  // No more seeding of demo users
  return Promise.resolve();
};

module.exports = seedUsers;
